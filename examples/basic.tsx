import React from 'react';
import { render } from 'ink';
import { MultilineInput } from '../src/index';

const App = () => {
  return (
    <MultilineInput
      // Add props here
    />
  );
};

render(<App />);
